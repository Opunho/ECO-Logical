<%= link_to "Calculate", fetch_expenses_path%>
<ul>
  <% @expenses.each do |expense| %>
    <li>
      <%= expense.external_id %>
      <%= expense.amount %>
      <%= expense.creditor_name %>
      <%= expense.creditor_id %>
    </li>
  <% end %>
</ul>
